# Cloudflare Pages serves extensionless URLs natively and 301s /page.html -> /page.
# Do NOT add /page -> /page.html rules here: that creates an infinite redirect loop.
# (These rules existed for Netlify and broke /refunds.html on Cloudflare.)

/home              /                 301
/*                 /404.html         404
